// TODO: websocket wrapper!
var socket

// var socket = new WebSocket('ws://' + window.location.host + window.location.pathname);
// window.onload = function() {
  // var show = function(el) {
  //   return function(msg) { el.innerHTML = msg + '<br />' + el.innerHTML; };
  // }(document.getElementById('msgs'));

  // TODO: delete when unneeded
  // var sender = function(form) {
  //   var input     = document.getElementById('input');
  //   input.onclick = function() { input.value = "" };
  //   form.onsubmit = function() {
  //     socket.send(input.value);
  //     input.value = "send a message";
  //     return false;
  //   }
  // }(document.getElementById('form'));


//   socket.onopen    = function()  { show('websocket opened'); };
//   socket.onclose   = function()  { show('websocket closed'); };
//   socket.onmessage = function(m) { show('websocket message: ' + m.data); };
// }
